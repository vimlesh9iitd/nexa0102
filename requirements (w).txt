# ═══════════════════════════════════════════════════════════
#  NEXA — AI Platform  |  requirements.txt
#  All packages required to run app.py on Streamlit Cloud
# ═══════════════════════════════════════════════════════════

# ── CORE FRAMEWORK ─────────────────────────────────────────
streamlit>=1.32.0

# ── HTTP / API CALLS ───────────────────────────────────────
requests>=2.31.0

# ── PDF GENERATION ─────────────────────────────────────────
reportlab>=4.1.0

# ── PDF READING (syllabus upload in Study Planner) ─────────
pdfplumber>=0.10.3
pypdf>=4.1.0

# ── EXCEL ──────────────────────────────────────────────────
openpyxl>=3.1.2
pandas>=2.2.0

# ── WORD DOCUMENTS ─────────────────────────────────────────
python-docx>=1.1.0

# ── IMAGE PROCESSING ───────────────────────────────────────
Pillow>=10.2.0

# ── AI IMAGE GENERATION (IMAGINE section) ──────────────────
# NOTE: torch + diffusers are large (~2 GB).
# If Streamlit Cloud deploy fails/times out, comment these 3 lines.
diffusers>=0.27.0
torch>=2.2.0
accelerate>=0.27.0
